ON t.IDStation = tb.IDStation
GROUP BY t.IDStation')
}
print(index)
}
totalMissingFromBeginning
totalMissingFromBeginning <- NULL
temptotal <- NULL
for(index in 1:(lastyear-startyear+1)) {
if(index == 1) {
totalMissingFromBeginning <- tableMissingDatasTotal2
} else {
temp <- tableMissingDatasTotal2[[index]]
temptotal<- totalMissingFromBeginning[[index-1]]
totalMissingFromBeginning <- sqldf('SELECT t.IDStation, t.NameStation,
SUM(t.MissingAmmonia+tb.MissingAmmonia),
SUM(t.MissingPM10+tb.MissingPM10),
SUM(t.MissingPM25+tb.MissingPM25),
SUM(t.MissingAllThree+tb.MissingAllThree)
FROM temptotal tb
JOIN temp t
ON t.IDStation = tb.IDStation
GROUP BY t.IDStation')
}
print(index)
}
totalMissingFromBeginning
tableMissingDatasTotal2
tableMissingDatasTotal2
totalMissingFromBeginning <- NULL
temptotal <- NULL
for(index in 1:(lastyear-startyear+1)) {
if(index == 1) {
totalMissingFromBeginning <- tableMissingDatasTotal2
} else {
temp <- tableMissingDatasTotal2[[index]]
temptotal<- totalMissingFromBeginning[[index-1]]
totalMissingFromBeginning <- sqldf('SELECT t.IDStation, t.NameStation,
SUM(t.MissingAmmonia+tb.MissingAmmonia),
SUM(t.MissingPM10+tb.MissingPM10),
SUM(t.MissingPM25+tb.MissingPM25),
SUM(t.MissingAllThree+tb.MissingAllThree)
FROM temptotal tb
JOIN temp t
ON t.IDStation = tb.IDStation
GROUP BY t.IDStation')
}
}
totalMissingFromBeginning <- NULL
temptotal <- NULL
for(index in 1:(lastyear-startyear+1)) {
if(index == 1) {
totalMissingFromBeginning <- tableMissingDatasTotal2
} else {
temp <- tableMissingDatasTotal2[[index]]
temptotal<- totalMissingFromBeginning[[index-1]]
totalMissingFromBeginning <- sqldf('SELECT t.IDStation, t.NameStation,
SUM(t.MissingAmmonia+tb.MissingAmmonia),
SUM(t.MissingPM10+tb.MissingPM10),
SUM(t.MissingPM25+tb.MissingPM25),
SUM(t.MissingAllThree+tb.MissingAllThree)
FROM temptotal tb
JOIN temp t
ON t.IDStation = tb.IDStation
GROUP BY t.IDStation')
}
}
totalMissingFromBeginning <- NULL
temptotal <- NULL
for(index in 1:(lastyear-startyear+1)) {
if(index == 1) {
totalMissingFromBeginning <- tableMissingDatasTotal2
} else {
temp <- tableMissingDatasTotal2[[index]]
temptotal<- totalMissingFromBeginning[[index]]
totalMissingFromBeginning <- sqldf('SELECT t.IDStation, t.NameStation,
SUM(t.MissingAmmonia+tb.MissingAmmonia),
SUM(t.MissingPM10+tb.MissingPM10),
SUM(t.MissingPM25+tb.MissingPM25),
SUM(t.MissingAllThree+tb.MissingAllThree)
FROM temptotal tb
JOIN temp t
ON t.IDStation = tb.IDStation
GROUP BY t.IDStation')
}
}
totalMissingFromBeginning <- NULL
temptotal <- NULL
for(index in 1:(lastyear-startyear+1)) {
if(index == 1) {
totalMissingFromBeginning <- tableMissingDatasTotal2
} else {
temp <- tableMissingDatasTotal2[[index]]
temptotal<- totalMissingFromBeginning[[index]]
totalMissingFromBeginning <- sqldf('SELECT t.IDStation, t.NameStation,
SUM(t.MissingAmmonia+tb.MissingAmmonia),
SUM(t.MissingPM10+tb.MissingPM10),
SUM(t.MissingPM25+tb.MissingPM25),
SUM(t.MissingAllThree+tb.MissingAllThree)
FROM temptotal tb
JOIN temp t
ON t.IDStation = tb.IDStation
GROUP BY t.IDStation')
}
print(index)
}
for(index in 1:(lastyear-startyear+1)) {
print(index)
}
totalMissingFromBeginning <- NULL
temptotal <- NULL
for(index in 1:(lastyear-startyear+1)) {
if(index == 1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[1]]
} else {
temp <- tableMissingDatasTotal2[[index]]
temptotal<- totalMissingFromBeginning[[index-1]]
totalMissingFromBeginning <- sqldf('SELECT t.IDStation, t.NameStation,
SUM(t.MissingAmmonia+tb.MissingAmmonia),
SUM(t.MissingPM10+tb.MissingPM10),
SUM(t.MissingPM25+tb.MissingPM25),
SUM(t.MissingAllThree+tb.MissingAllThree)
FROM temptotal tb
JOIN temp t
ON t.IDStation = tb.IDStation
GROUP BY t.IDStation')
}
}
totalMissingFromBeginning
tableMissingDatasTotal2
totalMissingFromBeginning
tableMissingDatasTotal2
size(tableMissingDatasTotal2)
length(tableMissingDatasTotal2)
for(index in 1:length(tableMissingDatasTotal2)) {
print(index)
}
for(index in 1:length(tableMissingDatasTotal2)) {
if(index==1) {
print("Ciao")
}
print(index)
}
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:length(tableMissingDatasTotal2)) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
}
print(index)
}
totalMissingFromBeginning
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:length(tableMissingDatasTotal2)) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
}
temp <- tableMissingDatasTotal2[[index]]
print(index)
}
temp
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:length(tableMissingDatasTotal2)) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT *
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
')
}
}
totalMissingFromBeginning
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:length(tableMissingDatasTotal2)) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT t.NameStation
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
}
totalMissingFromBeginning
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:length(tableMissingDatasTotal2)) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
}
totalMissingFromBeginning
tableMissingDatasTotal2
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:length(tableMissingDatasTotal2)) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia)
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
}
auxiliaryTable
auxiliaryTable
tableMissingDatasTotal2
auxiliaryTable
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:length(tableMissingDatasTotal2)) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia)
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
auxiliaryTable
totalMissingFromBeginning
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:length(tableMissingDatasTotal2+1)) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia)
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
auxiliaryTable
totalMissingFromBeginning
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:length(tableMissingDatasTotal2+1)) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia)
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:length(tableMissingDatasTotal2+1)) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia),
SUM(t.MissingPM10+a.MissingPM10),
SUM(t.MissingPM25+a.MissingAmmoniaPM25),
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
totalMissingFromBeginning
otalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:(length(tableMissingDatasTotal2)+1)) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia),
SUM(t.MissingPM10+a.MissingPM10),
SUM(t.MissingPM25+a.MissingAmmoniaPM25),
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
totalMissingFromBeginning
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:(length(tableMissingDatasTotal2))) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia),
SUM(t.MissingPM10+a.MissingPM10),
SUM(t.MissingPM25+a.MissingAmmoniaPM25)
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:(length(tableMissingDatasTotal2))) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia)
as MissingAmmonia,
SUM(t.MissingPM10+a.MissingPM10)
as MissingPM10 ,
SUM(t.MissingPM25+a.MissingAmmoniaPM25)
as MissingPM25
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
totalMissingFromBeginning
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:(length(tableMissingDatasTotal2))) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia)
as MissingAmmonia,
SUM(t.MissingPM10+a.MissingPM10)
as MissingPM10 ,
SUM(t.MissingPM25+a.MissingAmmoniaPM25)
as MissingPM25
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
totalMissingFromBeginning
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:(length(tableMissingDatasTotal2))) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
auxiliaryTable
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia)
as MissingAmmonia,
SUM(t.MissingPM10+a.MissingPM10)
as MissingPM10 ,
SUM(t.MissingPM25+a.MissingAmmoniaPM25)
as MissingPM25
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:(length(tableMissingDatasTotal2))) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
auxiliaryTable
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia)
as MissingAmmonia,
SUM(t.MissingPM10+a.MissingPM10)
as MissingPM10 ,
SUM(t.MissingPM25+a.MissingPM25)
as MissingPM25
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
totalMissingFromBeginning
auxiliaryTable
tableMissingDatasTotal2
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:(length(tableMissingDatasTotal2))) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
auxiliaryTable
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia)
as MissingAmmonia,
SUM(t.MissingPM10+a.MissingPM10)
as MissingPM10 ,
SUM(t.MissingPM25+a.MissingPM25)
as MissingPM25,
SUM(t.MissingAllThree+a.MissingAllThree)
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
totalMissingFromBeginning
totalMissingFromBeginning <- NULL
temp <- NULL
for(index in 1:(length(tableMissingDatasTotal2))) {
if(index==1) {
totalMissingFromBeginning <- tableMissingDatasTotal2[[index]]
} else {
temp <- tableMissingDatasTotal2[[index]]
auxiliaryTable <- totalMissingFromBeginning
auxiliaryTable
totalMissingFromBeginning <- sqldf('SELECT t.IDStation,t.NameStation,
SUM(t.MissingAmmonia+a.MissingAmmonia)
as MissingAmmonia,
SUM(t.MissingPM10+a.MissingPM10)
as MissingPM10 ,
SUM(t.MissingPM25+a.MissingPM25)
as MissingPM25,
SUM(t.MissingAllThree+a.MissingAllThree)
as t.MissingAllThree
FROM temp t
JOIN auxiliaryTable a
ON t.IDstation = a.IDStation
GROUP BY t.IDStation
')
}
print(index)
}
totalMissingFromBeginning
("/Users/marcovinciguerra/Github/GitTesi")
write.table(totalMissingFromBeginning, "MissingFromTheBeginning.csv")
setwd("/Users/marcovinciguerra/Github/GitTesi")
write.table(totalMissingFromBeginning, "MissingFromTheBeginning.csv")
setwd("/Users/marcovinciguerra/Github/GitTesi/DownloadData")
write.table(totalMissingFromBeginning, "MissingFromTheBeginning.csv")
